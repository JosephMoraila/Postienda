#pragma once
#include <wx/wx.h>

/**
 * @brief Returns the path to the resources folder, which contains images, icons, and other assets used by the application.
 * @return settings/resources/ <- Includes ten slash at the end
 */
std::string RES_FOLDER_PATH();




